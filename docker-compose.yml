version: '2'

networks:
  network-tier:
    driver: host

services:
  postgresql:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: aegis
    ports:
      - "5432:5432"